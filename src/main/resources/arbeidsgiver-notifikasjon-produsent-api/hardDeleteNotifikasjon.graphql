mutation HardDeleteNotifikasjon(
    $merkelapp: String!,
    $eksternId: String!
) {
    hardDeleteNotifikasjonByEksternId_V2(merkelapp: $merkelapp, eksternId: $eksternId) {
        __typename
        ... on Error {
            feilmelding
        }
    }
}
